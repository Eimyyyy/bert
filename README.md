# bert
# 0.环境要求
python = 3.11

```
pip install -r requirements.txt
```

# 1.项目概述
本项目实现了基于BERT (Transformer) 架构的文本分类器，用于区分 20 Newsgroups 数据集中的两个语义接近的类别：alt.atheism（无神论）与 soc.religion.christian（基督教）。

# 2.实验配置
## 2.1 数据划分
- **原始训练集大小**：1079
- **拆分后训练集**：863 (80%)
- **验证集**：216 (20%)
- **测试集**：717
## 2.2 参数
| 参数名称 | 数值 | 说明 |
| :--- | :--- | :--- |
| **vocab_size** | 动态 | 由预处理生成的 `word_to_idx` 词典长度决定 |
| **max_len** | **256** | 最大输入序列长度（超过截断，不足补 0） |
| **hidden_size** | **256** | 词嵌入维度及全连接层宽度 |
| **n_layers** | **6** | Transformer Encoder Block 的堆叠层数 |
| **attn_heads** | **8** | 多头自注意力机制的头数 ($d_k = 32$) |
| **dropout** | **0.1** | Transformer 内部层、注意力权重、嵌入层的丢弃率 |

# 3.文件说明
- `main.py`: 主函数。
- `best_bert_model.pth`: 训练过程中保存的验证集最优模型权重。
- `20news_processed.pkl`: 预处理后的分词数据。

# 4.运行方法
直接运行main.py即可。

# 5.BERT与GRU结果对比

| 模型 | 最佳验证集 Acc | 最终测试集 Acc | 泛化差距 (Val-Test) |
| :--- | :---: | :---: | :---: |
| **GRU** | 80.09% | 72.52% | 7.57% |
| **BERT** | **85.19%** | **77.82%** | **7.37%** |

**对比分析**：
- **性能上限**：GRU通过更新门和重置门来决定保留信息和接收信息，当前的隐藏状态 $h_t$ 依赖于上一个隐藏状态 $h_{t-1}$，在处理长文本时，早期信息不可避免地会被稀释，而BERT模型利用注意力机制，直接计算任意两个token的相关程度，对于无神论和基督徒新闻这种需要全文注意的文本数据，BERT效果优于GRU。
- **显存占用**：在实验过程中，BERT显存占用明显高于GRU。原因在于BERT采用多头注意力机制，计算复杂度与序列长度平方相关，而GRU逐步处理，计算复杂度与序列长度线性相关。

